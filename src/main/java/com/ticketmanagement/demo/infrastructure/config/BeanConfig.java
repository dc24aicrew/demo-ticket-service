package com.ticketmanagement.demo.infrastructure.config;

import org.springframework.context.annotation.Configuration;

/**
 * Bean configuration for wiring up clean architecture components
 * Creates beans for services, adapters, and other components
 */
@Configuration
public class BeanConfig {
    // This will be populated with @Bean methods to create service and repository instances
    // as we develop concrete implementations
}
